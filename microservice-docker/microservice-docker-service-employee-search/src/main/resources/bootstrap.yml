server:
  port: 8085
spring:
  application:
    name: service-employee-search
  cloud:
    config:
      uri: http://localhost:9090
  profiles:
    active: development
security:
  basic:
    enabled: false
management:
  security:
    enabled: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9091/eureka